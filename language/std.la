id = (\x.x)

trbool = (\x.\y.x)
flbool = (\x.\y.y)

add = (\a.\b.(+ a b))
sub = (\a.\b.(- a b))
mul = (\a.\b.(* a b))
div = (\a.\b.(/ a b))

equ = (\a.\b.(== a b))

not = (\b. if b then fls else tru)

inc = add 1
dec = (\n.sub n 1)

ass = (\x.(state = x))

square = (\n.mul n n)
cube = (\n.mul (square n) n)

mocking = (\f.f f)
cardinal = (\a.\b.\c.a c b)


nil